1. PostgreSQL is deployed on port {{ .Values.postgresql.service.port }}.
2. RabbitMQ is deployed on port {{ .Values.rabbitmq.service.port }} with management UI on port {{ .Values.rabbitmq.service.managementPort }}.
3. RabbitMQ management UI is accessible at http://{{ (index .Values.rabbitmq.ingress.hosts 0).host }}/ if Ingress is enabled.
